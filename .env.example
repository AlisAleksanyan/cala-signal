# Server-only credentials. Copy to .env.local; never commit real values.
OPENAI_API_KEY=
CALA_API_KEY=
OPENAI_MODEL=gpt-5.6-terra
# Long random server-only value used to sign short-lived, IP-bound scout tokens.
SCOUT_TOKEN_SECRET=
